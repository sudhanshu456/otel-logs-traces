{
    "name": "microservices",
    "version": "1.0.0",
    "scripts": {
        "start:user-service": "cd user-service && npm install && npm start",
        "start:cart-service": "cd shopping-cart-service && npm install && npm start",
        "start:all": "npm-run-all --parallel start:user-service start:cart-service"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
